server:
  port: 8081
  
spring:
  h2:
    console:
      enabled: true
      path: /private/h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  security:
    user:
      name: user
      password: password